name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: write
    uses: owncloud/reusable-workflows/.github/workflows/release.yml@main
    with:
      app-name: files_external_dropbox
      artifact-glob: build/dist/files_external_dropbox.tar.gz
    secrets:
      SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
      SIGNING_CERT: ${{ secrets.SIGNING_CERT }}
      SIGNING_CHAIN: ${{ secrets.SIGNING_CHAIN }}
